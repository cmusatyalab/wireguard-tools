import * as core from '../../core';
import Offcanvas from '../../../../js/bootstrap/mdb-prefix/offcanvas';
import initMDB from '../../../../js/autoinit/index.pro';

initMDB({ Offcanvas });

const compiled = { ...core.default, Offcanvas };
export default compiled;
