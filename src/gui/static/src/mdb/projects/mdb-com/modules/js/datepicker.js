import * as core from '../../core';
import Datepicker from '../../../../js/pro/datepicker';
import initMDB from '../../../../js/autoinit/index.pro';

initMDB({ Datepicker });

const compiled = { ...core.default, Datepicker };
export default compiled;
