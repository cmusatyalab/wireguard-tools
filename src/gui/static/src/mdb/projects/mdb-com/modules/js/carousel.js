import * as core from '../../core';
import Carousel from '../../../../js/free/carousel';
import initMDB from '../../../../js/autoinit/index.pro';

initMDB({ Carousel });

const compiled = { ...core.default, Carousel };
export default compiled;
