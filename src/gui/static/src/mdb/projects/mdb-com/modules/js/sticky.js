import * as core from '../../core';
import Sticky from '../../../../js/pro/sticky';
import initMDB from '../../../../js/autoinit/index.pro';

initMDB({ Sticky });

const compiled = { ...core.default, Sticky };
export default compiled;
